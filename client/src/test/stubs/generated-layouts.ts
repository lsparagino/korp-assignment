/**
 * Stub for the 'virtual:generated-layouts' Vite virtual module.
 * Used by vitest resolve.alias to prevent import failures in tests.
 */
export function setupLayouts(routes: unknown[]) {
    return routes
}
